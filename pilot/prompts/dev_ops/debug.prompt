{% set error_message = "You wanted me to check this - `" ~ issue_description ~ "` but there was a problem" %}
{% set success_message = "Now, we need to debug this issue" %}
{% set command_message = "and we need to be able to execute `" ~ command ~ "` successfully." if command else '' %}
{% set user_input_message = "I looked into this issue. I want you to take my findings as an important part of debugging this issue. Here are my findings:" ~ user_input ~ "" if user_input else '' %}

<h2>Debugging Steps</h2>
<ol>
  {% for step in steps_list %}
    <li>
      {% if step.type == 'command' %}
        Run the following command: <code>{{ step.command }}</code> (current working directory is project root folder)
      {% elif step.type == 'code_change' %}
        Implement the following code change: <pre>{{ step.change }}</pre>
      {% elif step.type == 'human_intervention' %}
        Human intervention required: <pre>{{ step.instruction }}</pre>
      {% endif %}
      {% if step.check_if_fixed %}
        Check if the issue is fixed.
      {% endif %}
    </li>
  {% endfor %}
</ol>

<p>{{ execution_order }}</p>

<p>{{ file_size_limit }}</p>

<p>{{ user_input_message }}{{ error_message }}{{ success_message }}{{ command_message }}</p>
